[package]
name = "chisel-py"
version = "0.1.0"
edition = "2021"
# MSRV mirrors the root crate (1.82, from Option::is_none_or in chisel's
# page_cache). PyO3 0.22 itself only needs 1.63, but the path-dep on the
# root crate makes its floor ours too.
rust-version = "1.82"
authors = ["Christophe Pettus <cpettus@pgexperts.com>"]
description = "PyO3 binding for the Chisel transactional storage engine"
license = "MIT"
repository = "https://github.com/Xof/chisel"
readme = "README.md"
keywords = ["database", "storage", "embedded", "pyo3", "python"]
categories = ["database-implementations", "api-bindings"]
# Published to PyPI as `chisel` via maturin; this Cargo crate
# (`chisel-py`) is the build artifact, not the Rust crates.io
# distribution surface.
publish = false

[lib]
name = "_chisel"
crate-type = ["cdylib"]

[dependencies]
chisel = { path = ".." }
# I75 (ISSUES.md, 2026-05-22): bumped from 0.22 to clear
# RUSTSEC-2025-0020 (Risk of buffer overflow in PyString::from_object;
# patched in 0.24.1+). Bumped again to 0.29 to clear RUSTSEC-2026-0176
# (out-of-bounds read in PyList/PyTuple nth iterators) and RUSTSEC-2026-0177
# (missing Sync bound on PyCFunction::new_closure), both patched in 0.29.0.
# This binding uses neither vulnerable API, but cargo-audit flags the crate
# transitively. abi3-py311 means the wheel runs against any Python 3.11+ via
# the stable ABI — that floor is preserved here.
pyo3 = { version = "0.29", features = ["extension-module", "abi3-py311"] }
